import 'package:flutter/material.dart';

class PrintScreens extends StatelessWidget {
  const PrintScreens({super.key});
  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Print Screen"),
      ),
    );
  }
}
